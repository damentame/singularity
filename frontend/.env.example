# Copy this file to frontend/.env and fill in values.
# For local dev: run `npx supabase start` and paste the API URL + anon key from its output.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<anon key from `npx supabase start` output>
